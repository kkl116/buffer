spring:
  sql:
    init:
      data-locations:
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
